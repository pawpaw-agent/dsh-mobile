{
  "name": "dsh-handheld",
  "version": "0.1.3",
  "private": true,
  "description": "DeepSeek Harness (dsh) Android client — full dsh web UI in a WebView over an SSH tunnel, plus a terminal mode"
}
